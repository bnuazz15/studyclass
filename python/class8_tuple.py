# t = (1, 2, 3)
# t = 1, 2, 3  # 괄호 생략 가능
# 원소가 1개인 경우, t = (a,) 이렇게 선언해줘야 함.
# student = ("name", 19, "CS")  --> packing
# name, age, major = student --> unpacking. name, age, major 에 각각 원소가 들어감
# + 연산 : tuple 끼리 concat하여 새로운 tuple 생성
# del t : tuple 자체 삭제
# del t[9], t[1] = 3 등 tuple 내 값을 변경하는 것은 불가
